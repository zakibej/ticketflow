# TicketFlow

Simple fullstack app:
- Frontend: HTML/CSS/JS
- Backend: Node.js + Express
- Auth: JWT
- DB: SQLite (local)

## Run locally
```bash
cd server
npm install
node server.js
```
Then open http://localhost:3000
